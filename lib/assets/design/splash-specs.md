# Splash Screen Configuration
# Create splash.png in assets/images with the following specs:
# - Size: 1242x2436 pixels (matches iPhone X resolution)
# - Dark background (#000000)
# - Centered app logo (200x200 pixels)
# - App name "SONIX HUB" in large text
# - Subtitle "Your Ultimate Entertainment Hub" in smaller text
# - Red accent color (#ff0000) for visual elements

The splash screen image should be created with these specifications and saved as splash.png in the assets/images directory.